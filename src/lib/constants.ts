/**
 * Shared project-level constants.
 * Kept in one module so UI copy, links and metadata cannot drift apart.
 */

/** Canonical GitHub repository URL used by the Header and Footer. */
export const GITHUB_REPO_URL = "https://github.com/Faralazu/isyara-webapp";

/** Public product name. */
export const APP_NAME = "Isyara";

/** Short tagline used next to the brand mark. */
export const APP_TAGLINE = "BISINDO AI";
